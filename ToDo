
[] create a gui that searches for images upon button press
    [x] create a gui with a text and button
        [x] add welcome text
        [x] main text will be "Select image to use"
        [x] button text will be "Select" 
    [] make button open file explorer for user to add image
        [] search from current folder
            * search only with image extensions i.e .png, .jpg. etc
        [] open file_explorer
        [] print the image path added in a new gui window, for now 