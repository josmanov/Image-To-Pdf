
CODE:
[x] refactor code into cleaner structure
[x] create a file to store constants and variables in for easier configuration and expansion
    [x] create constants and status files
    [x] update gui.py with new variables from constants..py & status.py files

GUI:
[x] create a gui that searches for images upon button press
    [x] create a gui with a text and button
        [x] add welcome text
        [x] main text will be "Select image to use"
        [x] button text will be "Select" 
    [x] make button open file explorer for user to add image
        [x] search from current folder
            * search only with image extensions i.e .png, .jpg. etc
        [x] open file_explorer
        [x] print the image path added in a new gui window, for now 
        [x] filter search for only images
        [x] add "Image selected" status on gui upon selecting image
        [x] make window start centered
        [x] add seperate colors for file selection status

OCR (TEXT FROM IMAGE EXTRACTOR):
[x] extract all text from image
    [x] use ocr to test a simple text extraction from an image
        [x] store string extracted from image
        [x] print string into terminal
    [x] test with large amounts of texts within a single image
    [x] create a loop to test all files within the tests/images folder
    [x] change ocr output from plain text to word boxes
    [x] get position data from detected words
    [x] test and print position data from detected words

PDF write module:
[x] create a pdf that contains selected image only
    [x] take image path and add .pdf suffix into variable
    [x] get image size
    [x] create pdf canvas using size
    [x] place image onto pdf add a second page 
    [x] save created pdf
    [x] test image to see resemblance of image and pdf file

IMAGE TO PDF:
[] add new textbox onto pdf using textbox data
    [x] convert image pixel coords into pdf coords
    [x] estimate font size from text box height
    [x] draw prepared wordboxes on top of image layer
    [x] test new image and text drawing
    [x] use clear high resolution image for better testing
    [x] fix dramatic difference in font size
    [] fix inconsistent box sizes
        [x] filter bottom box line to not end at descendant letters
        [x] fix texts without descendant letter unncessary bottom line uprise
        [x] fix descending symbols not being detected as descending
    [] fix font not ending evenly at the end of red box
    [] check examples of different sizes and bluriness of text to check resemblance
    